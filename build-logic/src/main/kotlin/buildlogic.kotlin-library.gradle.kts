plugins {
    kotlin("jvm")
    id("org.jetbrains.dokka")
    id("org.jetbrains.kotlinx.kover")
    id("com.diffplug.spotless")
}

// Basic configuration will be added via afterEvaluate or in individual modules
